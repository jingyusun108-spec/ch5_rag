# 5.1 概述 —— 检索增强生成（RAG）

本节为理论认知部分，无独立可运行的例题代码。

核心要点（详见 README.md「知识点速查」）：
1. 大模型四大固有痛点：知识截止、知识幻觉、无法访问私有领域知识、上下文窗口限制。
2. RAG 核心架构：先检索、再生成，把「死记硬背」交给检索系统，模型只负责理解与生成。
3. RAG 与微调：RAG 适合「给模型动态知识」，微调适合「教模型固定做事方法」，二者互补。

RAG 的完整代码落地见 5.2（向量存储）、5.3（文档检索）、5.4（综合实例）。
